{
  "name": "feature-generator",
  "version": "0.0.1",
  "description": "This schematic generates a pure component and it's container and if specified also a service",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && jasmine src/**/*_spec.js"
  },
  "keywords": [
    "schematics"
  ],
  "author": "",
  "license": "MIT",
  "schematics": "./src/collection.json",
  "dependencies": {
    "@angular-devkit/core": "^19.2.1",
    "@angular-devkit/schematics": "^19.2.1",
    "typescript": "~5.7.2"
  },
  "devDependencies": {
    "@types/node": "^18.18.0",
    "@types/jasmine": "~5.1.0",
    "jasmine": "^5.0.0"
  }
}
